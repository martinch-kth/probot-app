import * as React from 'react';
import { Theme } from '../theming';
export interface SvgWrapperProps {
    width: number;
    height: number;
    margin: {
        top?: number;
        right?: number;
        left?: number;
        bottom?: number;
    };
    defs?: any[];
    children: React.ReactNode | React.ReactNodeArray;
    theme: Partial<Theme>;
}
declare const SvgWrapper: React.SFC<SvgWrapperProps>;
export default SvgWrapper;
