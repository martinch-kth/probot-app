/// <reference types="recompose" />
declare const _default: ({ colorsKey, colorByKey, destKey, defaultColors, defaultColorBy, }?: {
    colorsKey?: string;
    colorByKey?: string;
    destKey?: string;
    defaultColors?: string;
    defaultColorBy?: string;
}) => import("recompose").ComponentEnhancer<{}, {}>;
/**
 * This HOC watch colors related props change
 * and returns the corresponding color generator function.
 * Using it prevent from having a new ref each time
 * we pass through the component, useful for shallow comparison.
 */
export default _default;
