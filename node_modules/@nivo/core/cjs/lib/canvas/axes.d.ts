import { Theme } from '../../theming';
export declare const renderAxisToCanvas: (ctx: CanvasRenderingContext2D, { width, height, position, scale, tickSize, tickPadding, tickRotation, format, theme, }: {
    width: number;
    height: number;
    position: string;
    scale: any;
    tickSize: number;
    tickPadding: number;
    tickRotation: number;
    format?: any;
    theme: Theme;
}) => void;
export declare const renderAxesToCanvas: (ctx: CanvasRenderingContext2D, { xScale, yScale, width, height, top, right, bottom, left, theme }: {
    xScale: any;
    yScale: any;
    width: any;
    height: any;
    top: any;
    right: any;
    bottom: any;
    left: any;
    theme: any;
}) => void;
export declare const renderGridLinesToCanvas: (ctx: CanvasRenderingContext2D, { width, height, scale, axis, }: {
    width: number;
    height: number;
    scale: any;
    axis: "x" | "y";
}) => void;
