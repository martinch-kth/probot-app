export declare const computeAxisTicks: ({ width, height, position: _position, scale, tickValues: _tickValues, tickSize, tickPadding, tickRotation, engine, }: {
    width: number;
    height: number;
    position: string;
    scale: any;
    tickValues?: number | (string | number | Date)[];
    tickSize: number;
    tickPadding: number;
    tickRotation: number;
    engine?: "svg" | "canvas";
}) => {
    x: number;
    y: number;
    ticks: any[];
    textAlign: string;
    textBaseline: string;
};
export declare const computeGridLines: ({ width, height, scale, axis, values, }: {
    width: number;
    height: number;
    scale: any;
    axis: "x" | "y";
    values?: (string | number | Date)[];
}) => {
    key: string;
    x1: number;
    x2: number;
    y1: number;
    y2: number;
}[];
