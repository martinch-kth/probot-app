export declare const getPolarLabelProps: (radius: number, angle: number, rotation: number, engine?: string) => {
    x: number;
    y: number;
    rotate: number;
    align: string;
    baseline: string;
};
